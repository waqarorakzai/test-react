pipeline {
    agent any

    stages {
        stage('Install Dependencies') {
            steps {
                script {
                    def tools = tool ' '
                    echo "Available tools: ${tools}"
                }
            }
        }
    }
}
